<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    Amazing grace of you my dear Lord.


    <!-- this is how to add variables in node -->


    <div class="container">
        <h1 class="mb-4">Blog Articles</h1>
        <a href="/articles/new" class="btn btn-success">New Article</a>
    </div>

    <% articles.forEach(article=> { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title">
                    <%= article.title %>
                </h4>
                <div class="card-subtitle text-muted mb-2">
                    <%= article.createdAt.toLocaleDateString() %>
                </div>

                <div class="card-subtitle mb-4">
                    <%= article.description %>
                </div>
                <br>

                <div>
                    <a href="articles/<%= article.slug %>" class="btn btn-primary">Read more</a>
                    <a href="/articles/edit/<%= article.id %>" class="btn btn-info">Edit</a>

                    <!-- For the delete button we don't want to have an <a href> because everythime google indexes our page, it would delete evertything from our DB -->
                    <!-- <a href="/articles/new" class="btn btn-fail">Delete</a> -->

                    <form action="/articles/<%= article.id %>?_method=DELETE" method="POST" class="d-inline">
                        <button type="submit" class="btn btn-danger">Delete</button>

                    </form>

                </div>

            </div>

        </div>
        <% }) %>

</body>

</html>